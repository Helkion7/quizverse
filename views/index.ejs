<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/matte-black.css" />
  </head>
  <body>
    <%- include('partials/header') %>

    <main class="container">
      <h1>Welcome to QuizVerse</h1>
      <p>Explore our collection of community-created quizzes</p>

      <section class="quizzes-section">
        <h2>Featured Quizzes</h2>

        <div class="quizzes-grid">
          <% if (quizzes && quizzes.length > 0) { %> <% quizzes.forEach(quiz =>
          { %>
          <div class="quiz-item slide-in">
            <div class="quiz-content">
              <h3><%= quiz.title %></h3>
              <p><%= quiz.description || 'No description available' %></p>
              <div class="quiz-meta">
                <span
                  ><%= quiz.questions ? quiz.questions.length : 0 %>
                  questions</span
                >
                <% if(quiz.author) { %>
                <span>By: <%= quiz.author.username || 'Anonymous' %></span>
                <% } %>
              </div>
            </div>
            <a href="/quiz/<%= quiz._id %>" class="quiz-link">
              <span class="view-quiz-btn">View Quiz</span>
            </a>
          </div>
          <% }) %> <% } else { %>
          <p class="no-quizzes">No quizzes available at the moment.</p>
          <% } %>
        </div>

        <div class="view-all">
          <a href="/quiz/browse" class="btn-minimal">View All Quizzes</a>
        </div>
      </section>
    </main>

    <%- include('partials/footer') %>
  </body>
</html>
