<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <%- include('partials/header') %>

    <div class="hero">
      <h1>Welcome to QuizVerse</h1>
      <p>Your interactive quiz platform for learning and fun</p>

      <div class="actions">
        <% if (user) { %>
        <a
          href="<%= user && user.role === 'admin' ? '/admin/dashboard' : '/user/dashboard' %>"
          class="btn btn-primary"
          >Go to Dashboard</a
        >
        <% } else { %>
        <a href="/auth/login" class="btn btn-primary">Login</a>
        <a href="/auth/register" class="btn">Register</a>
        <% } %>
      </div>

      <div class="featured-quizzes">
        <h2>Popular Quizzes</h2>
        <div class="quiz-list">
          <div class="card animate-fade-in">
            <h3>Programming Basics</h3>
            <p>Test your knowledge of programming fundamentals</p>
            <div class="quiz-meta">
              <span>15 questions</span>
              <span>500+ attempts</span>
            </div>
          </div>
          <div class="card animate-fade-in">
            <h3>Web Development</h3>
            <p>Challenge yourself with HTML, CSS and JavaScript questions</p>
            <div class="quiz-meta">
              <span>20 questions</span>
              <span>300+ attempts</span>
            </div>
          </div>
        </div>
      </div>
    </div>

    <%- include('partials/footer') %>
  </body>
</html>
